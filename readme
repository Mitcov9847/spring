# 📚 Library Management Application

##  Введение

В рамках данного проекта было разработано приложение для **управления библиотекой** с использованием **Spring Boot**, **JPA** и **PostgreSQL**.  
Основной задачей было создание **REST API**, которое позволяет выполнять операции CRUD (создание, чтение, обновление, удаление) с данными о:

- книгах,
- авторах,
- издателях,
- категориях,
- библиотеках.

Проект демонстрирует архитектуру, ориентированную на расширяемость и простоту сопровождения, с использованием современных Java-фреймворков.

---

## Описание проекта

Проект состоит из следующих взаимосвязанных сущностей:

- **Author (Автор)** — информация об авторах книг.
- **Publisher (Издатель)** — информация об издателях книг.
- **Book (Книга)** — данные о книге, включая название, год издания, категорию, автора и издателя.
- **Category (Категория)** — категория книги (например, *Фантастика*, *Научная литература*).
- **Library (Библиотека)** — данные о библиотеке, которая может содержать множество книг.

Каждая сущность оформлена в виде Java-класса с аннотациями JPA, что позволяет автоматически маппить их на таблицы базы данных.

---

## технологий

- **Java 17+**
- **Spring Boot**
- **Spring Data JPA**
- **PostgreSQL**
- **Maven**
- **Lombok**
- **Swagger (OpenAPI)** — для документирования REST API
- **Postman** — для тестирования API

---

## 📁 Этапы разработки

### 1. Инициализация проекта
Создан Spring Boot проект с использованием Maven. Добавлены зависимости для:

- Spring Web
- Spring Data JPA
- PostgreSQL Driver
- Lombok

### 2. Разработка сущностей
Созданы классы сущностей с аннотациями `@Entity`, `@ManyToOne`, `@OneToMany` и т.д.  
Организованы отношения между сущностями: `OneToMany`, `ManyToOne`, `ManyToMany` при необходимости.

### 3. Создание репозиториев
Для каждой сущности созданы интерфейсы репозиториев, расширяющие `JpaRepository`, что позволило:

- быстро реализовать стандартные CRUD-операции
- использовать query methods

### 4. Разработка контроллеров
Созданы REST-контроллеры для каждого ресурса:

- `GET`, `POST`, `PUT`, `DELETE` endpoints
- Использование DTO (Data Transfer Objects) для обмена данными
- Поддержка обработки ошибок и валидации данных

### 5. Интеграция с PostgreSQL
- Настроено подключение к базе данных через `application.properties`
- Выполнено автоматическое создание таблиц на основе JPA-сущностей

---

## Результат

В результате был создан полностью функциональный backend-проект с REST API, который позволяет:

- Управлять данными о книгах, авторах, издателях, библиотеках и категориях
- Хранить данные в PostgreSQL
- Расширять и масштабировать функциональность при необходимости

Проект может использоваться как основа для более сложных информационных систем, связанных с управлением библиотеками, книгами и каталогами.

